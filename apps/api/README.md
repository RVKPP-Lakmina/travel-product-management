# apps/api

The NestJS API for the Travel Product Management System — feature-folder
layout under `src/` (`products/`, `ai/`, `health/`, `common/`, `config/`).

See the [root README](../../README.md) for setup, architecture, and the
security-controls overview. Scripts: `pnpm start:dev`, `pnpm test`,
`pnpm build`.
